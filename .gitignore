node_modules
.svelte-kit
build
package

# Env
.env
.env.*
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OS / editor
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Misc
*.tsbuildinfo

# Claude Code (local-only settings; commit settings.json if shared with team)
.claude/settings.local.json
# CLAUDE.md — instrucciones locales del agente, no son parte del repo.
CLAUDE.md

# Söhne fonts (Klim Type Foundry, comercial — NO committear nunca).
# Cada developer tiene los .otf/.woff2 localmente bajo su propia licencia.
public/sohne/
